# Build prompt

From `books/`, build this reel with the local free-only ai-explainer pipeline. Preserve Kokoro `am_onyx`, render the Claude composer cold open and Your Turn beat, render all body concepts as native Manim scenes, compile 16:9, inspect the contact sheet and sampled frames, repair every blocker/major visual defect, write the QC report, and never publish.
